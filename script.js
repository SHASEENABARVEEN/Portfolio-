const circularProgress = document.querySelectorAll(".circular-progress"); 
Array.from(circularProgress).forEach((pb) => { const progressValue = pb.querySelector(".percentage"); const innerCircle = pb.querySelector(".inner-circle"); let startValue = 0, endValue = Number(pb.getAttribute("data-percentage")), speed = 50, progressColor = pb.getAttribute("data-progress-color"); const progress = setInterval(() => { startValue++; progressValue.textContent = `${startValue}%`; progressValue.style.color = `${progressColor}`; innerCircle.style.backgroundColor = `${pb.getAttribute( "data-inner-circle-color" )}`; pb.style.background = `conic-gradient(${progressColor} ${ startValue * 3.6 }deg,${pb.getAttribute("data-bg-color")} 0deg)`; if (startValue === endValue) { clearInterval(progress); } }, speed); });